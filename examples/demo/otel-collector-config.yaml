receivers:
  otlp:
    protocols:
      grpc:

exporters:
  file:
    path: ./out.json

  stdout:


processors:
  batch:

extensions:
  health_check:
  pprof:
    endpoint: :1888
  zpages:
    endpoint: :55679

service:
  extensions: [pprof, zpages, health_check]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [file, stdout]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [file, stdout]
